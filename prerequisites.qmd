# Before we start

## R’s anatomy

- read eval print loop
- console, editor
- packages, library
- CRAN
- .Rprofile
- workspace (don’t save it)

Packages needed to follow along, look at `deps.R`


Get comfortable with paths 

- absolute paths (no no)
- relative paths (yes)

`{here}` can help, but I won’t discuss it.

What is a text editor, and what is text? Word is not a text editor, and work files are
not text. Important to know the difference.
